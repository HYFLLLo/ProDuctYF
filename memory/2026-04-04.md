# 2026-04-04 Session Memory

## 项目：胡雨丰个人简介网站（Next.js + Framer Motion）

### 项目路径
`/Users/luanshixiaoxiongjackyoh/Desktop/Claw/Yvenn/portfolio`

### 技术栈
- Next.js 16.2.1 (App Router)
- Framer Motion
- TypeScript
- CSS Modules
- 开发端口：localhost:3000

### 今天完成的 UI/UX 优化

1. **数字滚动动画** (`CountUp` 组件) — IntersectionObserver + requestAnimationFrame，ease-out 曲线，2秒
2. **打字机效果** (`Typewriter` 组件) — 用于 Hero 区域 "Yven Hu" 名字，末尾闪烁光标
3. **文字解码效果** (`TextDecode` 组件) — requestAnimationFrame + 随机字符渐变，用于 Hero 4段自我介绍
4. **导航栏毛玻璃** — `backdrop-filter: blur(24px)` + 半透明背景，滚动时加深
5. **霓虹灯发光文字** (`neon` / `neon-cyan` / `neon-violet` CSS 类) — 多层 text-shadow + 闪烁动画
6. **波浪形分隔线** (`WaveDivider` 组件) — SVG 双波浪路径 + CSS 动画上下起伏，4条（Hero/Projects、Projects/Timeline、Timeline/Contact）
7. **极光背景** — 3个模糊渐变色块（绿松石/青色/紫罗兰）独立漂浮移动

### 微信卡片流光边框（最新）
- 点击导航"聊聊"按钮触发，滚动到 Contact 区域后给微信卡片加 4 秒霓虹灯边框效果
- 用 `box-shadow` 多层叠加（5层向外扩散）+ 快速闪烁动画（0.15s alternate）+ `neonBorderPulse` 关键帧（4秒渐强渐弱）
- 状态管理：`page.tsx` 的 `useState` → Nav `onWechatShine` → Contact `wechatShining` prop → 4秒后回调重置

### Hero 区域修改
- 角色文字固定为"具备的能力/素质"（删掉了原来的轮换动画）
- 去掉了 `useEffect` + `roleRef` 轮换逻辑
- 去掉了 `roles` 数组

### 已知问题
- `global-error.tsx` 在 Next.js 16 + Turbopack 下有兼容性问题，暂未创建

### 文件结构（关键新增/修改）
- `src/components/Animations/` — CountUp、Typewriter、TextDecode 动画组件
- `src/components/WaveDivider/` — 波浪分隔线
- `src/components/Contact/Contact.tsx` — 接收 `wechatShining` / `onShiningDone` props
- `src/app/page.tsx` — `'use client'` + `useState` 管理流光状态
- `src/app/globals.css` — neon glow 样式

---

## 2026-04-05（追加）

### Next.js 项目 Git 操作
- GitHub 仓库：`https://github.com/HYFLLLo/ProDuctYF`
- 分支：`main`
- 推送方式：SSH（已配置 SSH key，可直接 push）
- 最新 commit：`5bbc3a8`

### 项目详情数据位置
- 文件：`src/components/Projects/Projects.tsx`
- 数据：`projects` 数组，每个项目包含 `background`（背景）、`solution`（解决方案）、`result`（工作成果）三个字段
- 弹窗组件：`src/components/Projects/ProjectModal.tsx`
- 弹窗样式：`src/components/Projects/ProjectModal.module.css`
- 卡片样式：`src/components/Projects/Projects.module.css`

### 已填充的项目内容
- **项目一**：外卖夜宵爆品预测（PRD 已提供）
- **项目二**：智能客服系统（PRD 已提供）
- **项目三**：个人工作助手（PRD URL 已给，但飞书文档获取失败，需用户粘贴内容）

### 弹窗样式改进（今日）
- 弹窗宽度：480px → 560px
- 正文字号：0.82rem → 0.88rem
- `text-muted` → `text-secondary`（增加对比度）
- `white-space: pre-wrap` 保留换行符
- GitHub/Doc 链接：加 `{project.githubUrl !== '#'}` 条件渲染，placeholder 项目不显示链接
- Metric 数据：加 `{project.metric !== '—'}` 条件渲染，placeholder 项目不显示数据
- 弹窗 `max-height: 85vh` + `.body` 加 `overflow-y: auto`，内容超出时可滚动

### 卡片网格修复（今日）
- `.grid` 加 `align-items: stretch`，同排卡片高度统一
- `.inner` 加 `min-height: 0; overflow: hidden`
- `.highlights` 加 `max-height: 8rem; overflow-y: auto`，亮点列表过长时内部滚动

### 飞书文档获取
- 工具：`feishu_fetch_doc`（需要用户 OAuth 授权）
- 项目三文档：`https://my.feishu.cn/docx/MkWnd95QooqxNOxZpo2cabPhn0f`
- 状态：需要用户手动粘贴 PRD 内容
