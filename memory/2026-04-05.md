# 2026-04-05 Session Memory

## 今日完成的工作

### 1. 滚动条问题修复
- **问题**：详情卡片内容过多时，最外层卡片出现绿色滚动条
- **根因**：`.highlights` 列表设置了 `overflow-y: auto` 显示滚动条，且全局 CSS 定义了绿色滚动条样式
- **修复**：
  - `Projects.module.css` - 给 `.highlights` 添加隐藏滚动条样式
  - `ProjectModal.module.css` - 给 `.body` 添加 `overflow-x: hidden` 和隐藏滚动条样式
  - `ProjectModal.tsx` - 添加 `useEffect` 在弹窗打开时禁止 `body` 滚动

### 2. 详情卡片正文样式优化
- **问题**：正文是斜体、颜色对比度不够清晰
- **修复**：
  - `ProjectModal.module.css` - `.sectionText` 设置 `font-style: normal`，颜色改为 `rgba(240, 240, 245, 0.85)`
  - 后续优化：颜色改为 `var(--text-primary)`，对比度更高

### 3. 详情卡片渲染优化（最新）
- **优化内容**：
  - `.sectionNum`（01/02/03）颜色改为 `var(--accent-lime)` 绿色，更突出
  - `.sectionText` 颜色改为 `var(--text-primary)`（最亮白色）
  - 增加段落间距 `margin-bottom: 0.5rem`
  - 整体内边距增加 `padding: 1.2rem`
  - 行高增加到 `1.8`

### 4. 外卖夜宵爆品预测项目详情内容优化
- **背景**：精简为三大核心痛点
- **解决方案**：补充核心创新点
  - "静态动态数据分离"策略（节省 80%+ TOKEN）
  - 多维度爆品识别权重（热点事件 40%、用户偏好 25%、地域特色 20%、顶流热点 10%、趋势增长 5%）
  - 置信度机制（<70% 触发人工复核）
  - 平台差异化支持（美团 24小时 vs 淘宝 5天）
- **结果**：结构化呈现四大核心指标 + 架构优势总结

### 5. 智能客服系统项目详情内容修改
- 按用户要求修改：
  - 文档分块大小：500 字符 → 1000 字符
  - 移除 PostgreSQL 相关描述

### 6. 个人工作助手项目详情内容填充
- 按用户提供的 PRD 内容填充了 background、solution、result 三个字段

### 7. GitHub 推送
- Commit: `40364b1`
- 推送成功

---

## 项目文件路径
- 项目数据：`src/components/Projects/Projects.tsx`
- 弹窗组件：`src/components/Projects/ProjectModal.tsx`
- 弹窗样式：`src/components/Projects/ProjectModal.module.css`
- 卡片样式：`src/components/Projects/Projects.module.css`

## 外卖夜宵爆品预测项目深入研究摘要
- GitHub：`https://github.com/HYFLLLo/Late-night-snack-prediction`
- 架构：四层（数据层→分析层→决策层→展示层）
- 核心创新：静态动态数据分离、多维度爆品识别（5维度加权）、平台差异化
- 当前问题：场景推理准确率仅 6-8%（需接入 LLM 优化）
